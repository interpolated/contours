import * as THREE from 'three';
export interface Camera {
    bearing: number;
    center?: {
        lat: number;
        lng: number;
    };
    pitch: number;
    zoom: number;
}
export type MeshWithOneMaterial = THREE.Mesh<THREE.BufferGeometry, THREE.Material>;
