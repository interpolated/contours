import { FeatureId, RawSection, StackedSection } from './general';
import { LayerTree, ProjectLayer } from './layers';
import { ProjectAppEdits } from './project-apps';
import { Project } from './projects';
export type RawProjectPayload = {
    rawSections: Record<FeatureId, RawSection>;
    activeProject: Project;
    projectAppsByAppIDEdited: ProjectAppEdits;
    layerTree: LayerTree;
    projectLayers?: ProjectLayer[];
};
export type BakedProjectPayload = {
    stackedSections: StackedSection[];
    projectionOrigin: [number, number];
};
